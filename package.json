{
  "name": "michaelbossetta-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/sync-cv.mjs && eleventy",
    "start": "eleventy --serve",
    "mirror-assets": "node scripts/mirror-assets.mjs",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "wrangler": "^4.111.0"
  }
}
